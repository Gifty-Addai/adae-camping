# Cache built assets (JS, CSS, images, fonts) for 1 year
/assets/*
  Cache-Control: public, max-age=31536000, immutable

# Ensure index.html is never cached, so client gets new version immediately
/index.html
  Cache-Control: public, max-age=0, must-revalidate
